
<div class="show_all">
  <% @cocktails.each do |cocktail| %>
    <%= link_to cocktail do  %>
    <div class="card">
      <h2><%= cocktail.name %></h2>
      <%= cl_image_tag cocktail.photo.key, height: 300, width: 400, crop: :fill %>
    </div>
    <% end %>
  <% end %>
</div>



