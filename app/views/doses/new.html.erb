<h1 class="top_h1">New dose for "<%= @cocktail.name %>"</h1>
<hr>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all %>
  <%= f.submit "Add new dose" %>
<% end %>
